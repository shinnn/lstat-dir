{
  "name": "lstat-dir",
  "version": "2.0.2",
  "description": "Run `fs.lstat` for all contents in a given directory",
  "repository": "shinnn/lstat-dir",
  "author": "Shinnosuke Watanabe (https://github.com/shinnn)",
  "scripts": {
    "pretest": "eslint --fix --format=codeframe index.js test.js",
    "test": "nyc --reporter=html --reporter=text node test.js"
  },
  "license": "ISC",
  "files": [
    "index.js"
  ],
  "keywords": [
    "fs",
    "file",
    "lstat",
    "dir",
    "readdir",
    "info",
    "information",
    "map",
    "promise",
    "async"
  ],
  "dependencies": {
    "readdir-sorted": "2.0.0-2"
  },
  "devDependencies": {
    "@shinnn/eslint-config-node": "^4.0.2",
    "eslint": "^4.10.0",
    "nyc": "^11.3.0",
    "tape": "^4.8.0"
  },
  "eslintConfig": {
    "extends": "@shinnn/node"
  }
}
